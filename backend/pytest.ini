[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --tb=short
cache_dir = /tmp/pytest_cache
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow-running tests
